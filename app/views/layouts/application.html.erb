<!DOCTYPE html>
<html>
  <head>
    <title><%= yield(:title) %></title>
    <!-- CSS -->
    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= javascript_include_tag "application" %>

    <%= csrf_meta_tags %>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <!-- Favicons and touch icons -->
    <link rel="shortcut icon" href="/favicon.ico">

    <!-- Facebook Meta Tags -->
    <meta property="og:site_name" content="Just Flew In" />
    <meta property="og:type" content="website"/>
    <meta property="og:title" content="<%= yield(:title).present? ? yield(:title) : t('brand') %>"/>
    <meta property="og:description" content="Find stuff in new cities that are in line with your tastes and preferences back home."/>
    
    <!-- Google Meta Tags -->
    <meta name="google-site-verification" content="oMRdbHE1oJ-NeNVTtXNf18G25iwS078iR_TyNYPOZVs" />
    <meta name="description" content="Find stuff in new cities that are in line with your tastes and preferences back home. <%= yield(:title).present? ? yield(:title) : 'Find your neighborhood.' %>" />  
    
    <!-- Google Analytics -->
    <%= render 'common/google_analytics' %>
  </head>
  <body>
    <div id="fb-root"></div>
    <%= render 'common/header' %>    
    
    <div>
      <% flash.each do |key, value| %>
        <div class="alert alert-<%= key %>"><%= value %></div>
      <% end %>
      <%= yield %>
    </div>
    
    <%= render 'common/footer' %>
  </body>
</html>
